#include <stdio.h>

int main() {
    char letter;
    
    letter = 'a';
    letter = 97;    // 97 translates to 'a' in the
                    // ASCII table.
    
    letter = letter + 1;    // letter now has the
                            // value 'b'.


    return 0;
}